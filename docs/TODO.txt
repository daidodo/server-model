                SERVER TODO:
(DONE)1.改善消息队列，使可以一次pop出所有元素
(DONE)2.独立出accept的线程，减少poll或epoll线程的CPU占用率
(DONE)3.独立出send操作，专门负责发送回应
(DONE)4.优化服务器架构
(DONE)5.支持epoll和poll
(DONE)6.CPoll::RemoveFd和CEpoll::RemoveFd批量版本可以继续优化
(DONE)7.优化logsys::CLocalLogger，减少FlushStream()调用次数，例如增加CScopeLogger
	注意：CScopeLogger只能在关键地方使用，不能作为常用日志类
(DONE)8.为CInByteStream和COutByteStream增加manipulator
9.增加CInBitStream和COutBitStream，实现对位的操作
(CANCEL)10.logsys里的CWriteBuffer每次写文件固定为buf_sz_的大小，是否可以修改为自适应写入数据的大小
	这样会导致用户的设置无效
(DONE)11.用名字空间包裹整个代码
(CANCLE)12.CLockHashTable代码需要改进一下

                SERVER BUGS:
(FIXED)1.在服务器fd个数满了之后，accept线程会陷入死循环，浪费CPU
	等待1秒，不要死循环
(FIXED)2.CFdSockMap::GetSock函数实现有线程同步问题，改为返回对象，或传引用参数
(FIXED)3.CmdHandler线程里太频繁的执行Tools::GetTimeUs()，可能成为性能瓶颈
	通过配置，选择是否统计命令执行时间
(FIXED)4.CEpollServer::handleExpiredFd里没有从epoll_里删除fd

                SEVER TEST:


                BUSINESS TODO:


                BUSINESS BUGS:


                BUSINESS TEST:

